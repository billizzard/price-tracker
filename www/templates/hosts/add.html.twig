{% extends 'profile.html.twig' %}

{% block contentTitle %}
    {{ 'l.add'|trans }}
{% endblock %}

{% block breadcrumbAfter %}
    <li class="active">{{ 'l.add'|trans }}</li>
{% endblock %}

{% block content %}
    {{ form_start(form) }}
        {{ form_row(form.host, { 'label': 'l.host'|trans}) }}
        {{ form_row(form.logoFile) }}
        <button id="submit" type="submit" class="btn btn-primary">{{ 'l.add'|trans }}</button>
    {{ form_end(form) }}

{% endblock %}

